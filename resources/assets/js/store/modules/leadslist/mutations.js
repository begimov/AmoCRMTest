export default {
    setCount(state, value) {
        state.count = value;
    },
    setIsLoading(state, value) {
        state.isLoading = value;
    },
    setLeads(state, leads) {
        state.leads = leads;
    }
}